# Task T1-A — Add health endpoint

Plan: `plan.md`

## Build
Add GET /health that returns { ok: true }.

## Test
GET /health returns 200 and { ok: true }.

## Accept
curl the health endpoint returns ok.

## Environment
The environment cannot support the work. Fix it in this worktree — do not start a parallel repair elsewhere, and do not treat a hard build as an environment problem.

Summary: Cannot reach postgres.

Needs:
- DATABASE_URL must be set
- postgres must accept connections on 5432
