# Task T1-A — Add health endpoint

Plan: `plan.md`

## Build
Add GET /health that returns { ok: true }.

## Test
GET /health returns 200 and { ok: true }.

## Accept
curl the health endpoint returns ok.

## Prior attempt
The last attempt failed. Fix the blockers; do not expand scope.

Summary: Typecheck failed.

Blockers:
- src/api/health.ts: missing return type
