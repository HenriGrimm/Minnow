# Task T1-A — Add health endpoint

Plan: `plan.md`

## Build
Add GET /health that returns { ok: true }.

## Test
GET /health returns 200 and { ok: true }.

## Accept
curl the health endpoint returns ok.

## Why this is running again
builder-failed-twice

## Integration
Tip: c0ffee0123456789abcdef0123456789abcdef01

Merged tasks:
- T0-A: c0ffee012345

## What the final test found
Failed rung: typecheck
Ran: typecheck
command: npx tsc --noEmit
Run it from the root of your own task worktree. That worktree is branched from the board's integration branch `minnow/board/b1/integration`, at the tip the final test ran against, so it has the same files. The integration checkout itself is outside your workspace; do not look for it or cd into it.

error TS2322: Type number is not assignable to type string.

## What this task did before
- Typecheck failed on the first try.
- Typecheck failed again.
