{
  "id": "coding-smoke",
  "label": "Coding smoke tests",
  "version": 1,
  "tasks": [
    {
      "id": "list-src",
      "prompt": "List TypeScript files under src/agents (names only, no commentary).",
      "allowedTools": ["list_directory", "find_files"],
      "deniedTools": ["execute_command", "save_file"],
      "rubricPrompt": "Score 100 if the answer lists at least three .ts files under src/agents; 0 otherwise. Reply JSON only: {\"score\":number,\"pass\":boolean,\"rationale\":string}"
    },
    {
      "id": "search-agents",
      "prompt": "Use search_in_file to find the word SubAgentRunner under server/runner (file paths only, brief).",
      "allowedTools": ["search_in_file", "read_file", "find_files"],
      "deniedTools": ["execute_command", "save_file"],
      "rubricPrompt": "Score 100 if the answer mentions server/runner/turn-runner.js or createTurnRunner; 0 otherwise. Reply JSON only: {\"score\":number,\"pass\":boolean,\"rationale\":string}"
    }
  ]
}
